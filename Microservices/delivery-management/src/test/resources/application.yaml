spring:
  application:
    name: delivery-management
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/teste
    username: postgres
    password: postgres